import { Meta, Story } from "@storybook/addon-docs";
import meta from "./input-date.stories";
import { Default, Time, DateTime } from "./input-date.stories";

<Meta 
    title={meta.title}
    component={meta.component}
    decorators={meta.decorators}
    argTypes={meta.argTypes} />

# InputText

This is a nice way to input date!

<Story story={Default} />

First import `InputDateModule` from `@lens/ui-prime-components`.

```html
<lens-input-date id="foo" placeholder="test"></lens-input-date>
```

## DateTime

<Story story={DateTime} />

```html
<lens-input-date id="foo" placeholder="test" mode="datetime"></lens-input-date>
```

## Time

<Story story={Time} />

```html
<lens-input-date id="foo" placeholder="test" mode="time"></lens-input-date>
```
