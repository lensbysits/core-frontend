import { Meta, Story } from "@storybook/addon-docs";
import meta from "./accordion.stories";
import { Default, Spinner } from "./accordion.stories";

<Meta
    title={meta.title}
    component={meta.component}
    decorators={meta.decorators}
    argTypes={meta.argTypes} />

# Accordion

This is a nice accordion!

<Story story={Default} />

First import `AccordionModule` from `@lens/ui-prime-components`.

```html
	<lens-accordion>
		<accordion-panels>
			<accordion-panel header="tab1-header">
				<div>tab1 content</div>
			</accordion-panel>
			<accordion-panel header="tab2-header">
				<div>tab2 content</div>
			</accordion-panel>
			<accordion-panel header="tab3-header">
				<div>tab3 content</div>
			</accordion-panel>
		</accordion-panels>
	</lens-accordion>
```
