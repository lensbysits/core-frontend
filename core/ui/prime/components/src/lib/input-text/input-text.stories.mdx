import { Meta, Story } from "@storybook/addon-docs";
import meta from "./input-text.stories";
import { Default, WithIcon } from "./input-text.stories";

<Meta 
    title={meta.title}
    component={meta.component}
    decorators={meta.decorators}
    argTypes={meta.argTypes} />

# InputText

This is a nice way to input text!

<Story story={Default} />

First import `InputTextModule` from `@lens/ui-prime-components`.

```html
<lens-input-text id="foo" placeholder="test"></lens-input-text>
```

## With icon

<Story story={WithIcon} />

```html
<lens-input-text id="foo" icon="search" iconAlign="left|right" spinIcon="false|true"></lens-input-text>
```