import { Meta, Story } from "@storybook/addon-docs";
import meta from "./button.stories";
import { Default } from "./button.stories";

<Meta 
    title={meta.title}
    component={meta.component}
    decorators={meta.decorators}
    argTypes={meta.argTypes} />

# Button

This is a nice button!

<Story story={Default} />

First import `ButtonModule` from `@lens/ui-prime-components`.

```html
<lens-button label="Click me" type="secondary" (clicked)="buttonClicked()"></lens-button>
```